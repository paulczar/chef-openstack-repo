berksfile:

cookbooks:

roles:
- os-base:
- os-ops-messaging:

environments:
- example:

data bags:

nodes:
- ubuntu2-1204.vm:
    run_list: role[os-ops-messaging]
    options: -i ~/.ssh/mray.pem -x mray --sudo -E example
